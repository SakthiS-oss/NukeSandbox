location          = "eastus"
name_prefix       = "nukesandbox"
github_repository = "SakthiS-oss/NukeSandbox"

# Secrets are not Terraform variables. After apply:
#   az keyvault secret set --vault-name <key_vault_name> --name google-api-key --value "$GOOGLE_API_KEY"
#   az keyvault secret set --vault-name <key_vault_name> --name api-key-hashes --value "$API_KEY_HASHES"
